"""Training layer: trains, evaluates, compares, and persists ML models.

Consumes the engineered dataset produced by the feature engineering
layer (Sprint 5) and produces a trained, persisted model plus a
comparison report across candidate algorithms — ready for the
prediction layer (Sprint 7).
"""
