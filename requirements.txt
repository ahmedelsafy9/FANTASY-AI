# Core
pandas>=2.2,<3.0
numpy>=2.1.0
requests>=2.31,<3.0

# Machine learning (Sprint 6+)
scikit-learn>=1.4,<2.0
joblib>=1.3,<2.0
xgboost>=2.0,<3.0
lightgbm>=4.3,<5.0

# API (Sprint 8+)
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0
pydantic>=2.6,<3.0
slowapi>=0.1.9,<1.0

# API hardening (rate limiting)
slowapi>=0.1.9,<0.2

# Testing
pytest>=8.0,<10.0
pytest-cov>=5.0,<6.0
httpx>=0.27,<1.0

# Code quality
flake8>=7.0,<8.0
black>=24.0,<27.0
mypy>=1.9,<2.0

# ---------------------------------------------------------------------------
# The packages below are declared for forward compatibility with later
# sprints and are not required yet. They are commented out to keep the
# current environment minimal; uncomment as each sprint introduces them.
# ---------------------------------------------------------------------------
# psycopg2-binary>=2.9,<3.0     # PostgreSQL integration
